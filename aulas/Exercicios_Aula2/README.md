### **Atividade Prática**

1. Crie um arquivo `tipos.js`.

2. Declare variáveis com **todos os tipos primitivos**.

3. Mostre cada tipo no console usando `typeof`.

4. Faça 3 conversões de tipo:
    - De string para número.
    - De número para string.
    - De número para boolean.

5. Explique no final (em comentário) a diferença entre **valor primitivo** e **referência**.